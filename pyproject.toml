[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "datascout"
version = "3.0.0"
description = "AI Dataset Research Agent"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["datascout*"]
